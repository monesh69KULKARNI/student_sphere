import 'events_list_screen.dart';

// Alias for EventsListScreen to match home_screen.dart imports
class EventsScreen extends EventsListScreen {
  const EventsScreen({super.key}) : super(isPublicOnly: false);
}

