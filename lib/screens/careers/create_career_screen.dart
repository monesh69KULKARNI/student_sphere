import 'package:flutter/material.dart';

class CreateCareerScreen extends StatelessWidget {
  const CreateCareerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Post Career Opportunity'),
      ),
      body: const Center(
        child: Text('Create career - Coming soon'),
      ),
    );
  }
}

