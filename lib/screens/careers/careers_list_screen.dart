import 'package:flutter/material.dart';

class CareersListScreen extends StatelessWidget {
  const CareersListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Career Opportunities'),
      ),
      body: const Center(
        child: Text('Careers list - Coming soon'),
      ),
    );
  }
}

